<nav class="navbar navbar-expand-lg">
    <div class="container">
        <a class="navbar-brand" href="/">Logo</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/">Inicio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/view/products">Tienda online</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                {{#unless user}}
                <li class="nav-item">
                    <a href="/register" class="nav-link">Registrarse</a>
                </li>
                <li class="nav-item">
                    <a href="/login" class="nav-link">Iniciar sesion</a>
                </li>
                {{/unless}}

                {{#if user.cart}}
                <li class="nav-item">
                    <a href="/view/carts/{{user.cart}}" class="nav-link">Carrito</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a href="/view/carts/nullo" class="nav-link">Carrito</a>
                </li>
                {{/if}}

                {{#if user}}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        Mi perfil
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a class="dropdown-item" href="/profile" class="nav-link">{{user.name}} {{user.first_name}}
                                {{user.last_name}}</a>
                        </li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li>
                            <a class="dropdown-item" href="/chat" class="nav-link">Chat</a>
                        </li>
                        {{#if onlyAdmin}}
                        <li><a class="dropdown-item" href="/realtimeproducts">Administrar productos</a></li>
                        {{!-- <li><a class="dropdown-item" href="#">Administrar usuarios</a></li> --}}
                        {{/if}}
                        {{#if isPremium}}
                            <li><a class="dropdown-item" href="/newproduct">Añadir productos</a></li>
                            <li><a class="dropdown-item" href="/myproducts">Mis productos</a></li>
                        {{/if}}
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="/api/session/logout">Cerrar sesion</a></li>
                    </ul>
                </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>